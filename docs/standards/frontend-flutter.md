# Frontend Flutter Standards

- Organize the app by feature.
- Keep shared UI and app-wide services in `core`.
- Keep navigation centralized in the app layer.
- Separate presentation, state, and data access.
- Build reusable tokens and components for onboarding, profile, and chat flows.
